[project]
name = "timeseries-autoencoder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "notebook>=7.5.1",
    "pandas>=2.3.3",
    "pytest>=9.0.2",
    "pytest-mock>=3.15.1",
    "scikit-learn>=1.8.0",
    "tensorflow>=2.20.0",
]

[tool.pytest]
addopts = [
    "-v"
]
testpaths = [
    "tests",
]
pythonpath = [
    "src",
]
